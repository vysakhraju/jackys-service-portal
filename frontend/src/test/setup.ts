// Loaded once before every test file (see vitest.config.ts's setupFiles). Adds the
// toBeInTheDocument()/toHaveTextContent()/etc matchers RTL tests use throughout.
import '@testing-library/jest-dom/vitest';
